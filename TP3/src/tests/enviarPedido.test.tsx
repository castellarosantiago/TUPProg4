// import { render, screen } from '@testing-library/react';
// import userEvent from '@testing-library/user-event';
// import App from '../App';

// test('envia pedido y muestra confirmacion', async () => {
//   render(<App />);
//   await screen.findByText('Café');
//   await userEvent.click(screen.getAllByRole('button', { name: /agregar/i })[0]);
//   await userEvent.click(screen.getByRole('button', { name: /enviar pedido/i }));
//   await screen.findByText(/Pedido confirmado/i);
//   expect(screen.queryByRole('list', { name: /pedido/i })).toBeEmptyDOMElement();
// });
